kakao:
  client:
    id: "10fea61c27520fa86be5ee9bb25e158b"
  redirect:
    uri: "http://localhost:8080/oauth2/kakao/callback"

naver:
  client:
    id: "qWh_rcXFfAta4rJPdMSC"
    secret: "NlBQNaIMN0"
  redirect:
    uri: "http://localhost:8080/oauth2/naver/callback"

server:
  port: 80

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:mem:test'

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        show_sql: true